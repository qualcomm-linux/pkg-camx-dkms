# This file was generated by autodep8(1) from the camx-dkms binary package:
#
#   $ autodep8 > debian/tests/control
#
# It is kept in the tree so the generated test can be adjusted; autopkgtest
# only falls back to autodep8 when debian/tests/control is absent.
#
# The dkms-autopkgtest run below is disabled: it cannot pass in debusine,
# where the testbed only offers stock Debian kernels rather than the Qualcomm
# kernel camx is built against, so the module fails to build for every kernel
# it finds.
# Commenting out every field, rather than deleting this file, is what keeps
# the generated test from coming back.
#
# TODO: re-enable once the testbed can provide a kernel camx builds against.
#Test-Command: /usr/lib/dkms/dkms-autopkgtest
#Restrictions: needs-root, breaks-testbed, allow-stderr, superficial, 
#Depends: dkms, openssl, linux-doc, dctrl-tools, 
#Features: test-name=dkms-autopkgtest
